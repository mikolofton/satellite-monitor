To run the application, install by running

```
$ mvn clean install
```

This will produce a jar in the target folder which can be executed by running

```
$ java -jar target/launch-satellite-monitor.jar /path/to/file
```
